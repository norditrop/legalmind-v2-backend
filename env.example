# Variáveis de ambiente de exemplo para o LegalMind Web Investigation Backend

# Define a porta da aplicação
PORT=8000

# Chave da API se quiser usar ferramentas externas futuramente
API_KEY=your_api_key_here

# Timeout padrão para requisições web (em segundos)
DEFAULT_TIMEOUT=10
